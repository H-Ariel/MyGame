+ THROW_IF_FAILED
// throw if `x` faild. `x` should be HRESULT object or function with HRESULT return type

+ ליצור שלבים חדשים

התוכנה של המשחק:
* צלילים
* לסדר את התנועה של הרוח
* ליצור JSON לסוגי אריחים
* כפתורים
* אובייקטים ברקע (עננים, עצים, וכו')
+ להוסיף אופציה לפתיחת קובץ-שלב מהמחשב
* לשלב את הדחיסה של השלבים ?
* להוסיף עוד כוחות מיוחדים ולצייר משהו כשהם נמצאים
* לשים מהירויות לplayer&enemy מתוך ה-json
* שמירת שלבים "פתוחים" 

---------------------------------------
התוכנה שמכינה השלבים:
+ חלון אחד ומנועים מרובים
+ להציג את השחקן (תמונה אמיתית)
+ אופציה לבדוק שלב

---------------------------------------
לעשות מתישהו:
+ לבטל כמה שיותר אזהרות (גם ברמה 4)
+ לעשות clean ואז build לכל ה-slnים
